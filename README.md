Для хранения данных используется PostgreSQL. Настройки подключения к БД, необходимо указать в .env, в корневой директории.

1.              npm install
2.              npm run migrate-seed
3.              npm run dev
